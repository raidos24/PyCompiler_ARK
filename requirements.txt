# PyCompiler ARK++ (ARK++) — Runtime requirements

# Core GUI framework (Qt for Python)
# Matrix:
# - Python >= 3.13 => PySide6/shiboken6 >= 6.8 (adds 3.13 support)
# - Python <= 3.12 => prefer < 6.8 (keeps Qt6.7 ABI, avoids early 6.8 issues)
PySide6>=6.8.0; python_version >= "3.13"
shiboken6>=6.8.0; python_version >= "3.13"
PySide6<6.8; python_version < "3.13"
shiboken6<6.8; python_version < "3.13"

# System utilities
psutil>=5.9.0,<6.0.0

# Configuration and data formats
PyYAML>=5.4.1,<7.0.0
# tomli only needed on Python < 3.11 (tomllib builtin on 3.11+)
tomli>=2.0.1,<3.0.0; python_version < "3.11" 
jsonschema

# Image processing
# Pillow 11+ is needed for Python 3.13
Pillow>=11.0.0; python_version >= "3.13"
Pillow; python_version < "3.13"

# Test dependencies (dev)
# pytest 8.3+ adds Python 3.13 support
pytest>=8.3.0; python_version >= "3.13"
pytest; python_version < "3.13"
# pytest-qt 4.4+ supports pytest 8
pytest-qt>=4.4.0
# pytest-asyncio 0.23.6+ works with pytest 8
pytest-asyncio>=0.23.6
pytest-cov

# Code quality tools (dev)
# Use versions that support Python 3.13
black
ruff
mypy

# Security tools (dev)
bandit
pip-audit
safety
click
rich
colorama
